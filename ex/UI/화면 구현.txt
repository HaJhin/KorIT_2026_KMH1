6/29 평가
유형 : 포트폴리오

HTML
태그 종류
화면에서 태그들을 작성하면 어떻게 보이는지
속성 : 시작 태그에 들어가는 내용
href
src
alt
style

시맨틱 태그
특별한 기능이 있진 않지만 논리적으로 페이지의 구조를 나눌 때 사용
<header>
<main>
<section>
<aritcle>
<aside>
<footer>

CSS
테두리,폰트,정렬 등 페이지를 꾸미기 위해 사용하는 것

CSS 적용하는 법
1. 스타일을 적용할 태그의 선택
- <p style="속성값"> 내용 </p> : 태그에 직접 스타일 주기
- 페이지 내부에 <style> ~ </style> 안에 스타일 작성하기

선택자 { 스타일속성 : 속성값 }

선택자의 종류
- 태그를 직접 선택할 수 있다 : 내가 원하지 않는 것도 같이 스타일 적용
p { color : red; }

- 클래스를 부여한다 : 내가 스타일을 주고싶은 태그만 선택
<p class="c1"> 내용 </p>

.c1{ color : blue;}

- 아이디를 부여한다 : 아이디는 고유 속성이라 중복되면 안된다.
<p id="p_id"> 내용 </p>

#p_id{ color : green;}

- 속성 선택자
<input type="text"/>
<input type="password"/>
<input type="number"/>
<input type="email"/>

input[type="password"] { border : ...;}

스타일이 겹칠때 적용되는 우선순위
1. !important : 가장 우선순위가 높음
#txt { color : red !important; } 
2. 인라인 스타일
3. id 선택자
4. 클래스 선택자
5. 태그 선택자
6. 브라우저 기본 스타일

우선순위가 같으면 나중에 선언한 것이 적용이 된다

background

font

길이 단위 : px, em, rem, %, vh, vw

간격
외부간격 : margin
내부간격 : padding

화면에 요소를 어떻게 보여줄 것인가 : display

요소의 정렬 : flex
컨테이너 : flex 속성을 지정하는 요소
아이템 : flex 속성에 영향을 받는 요소

grid 레이아웃
- 화면을 가로줄과 세로줄이 잇는 표처럼 나누고, 그 안에 요소를 배치하는 레이아웃 방식이다.

flex vs grid
flex -> 한 줄 방향 배치에 강점이 있다
grid -> 행과 열을 동시에 다루는 2차원 배치에 강하다.

grid -> 행과 열을 동시에 다루는 2차원 배치에 강하다.

grid container : grid가 적용된 부모 요소
grid item : 컨테이너 안에 있는 자식 요소들
cell : 행과 열이 만나는 한 칸
grid line : 칸을 나누는 선
grid tack : 행 또는 열 하나를 의미
gap : 칸 사이의 간격
grid area : 여러 칸을 합친 영역

==============================================
06/23

flex : 한 줄씩 요소를 배치할 때 유리
grid : 여러 줄에 요소를 배치할 때 유리

가로줄과 세로줄로 나누어 배치하는 레이아웃 형식

컨테이너
grid 속성을 가지는 요소

아이템
grid 속성에 영향을 받는 요소

grid-template-columns  : 200px 200px 200px
- 세로칸, 즉 열의 개수와 크기를 정하는 속성

fraction(fr) : grid에서 자주 사용하는 단위
- 남은 공간을 비율로 나누는 단위

grid-template-rows : 100px 200px
- 행을 만들고 행의 높이를 정할 수 있다.

gap : 간격
- grid item 사이의 간격

repeat(개수,크기) 함수
- 같은 크기의 열을 반복해서 만들 때 사용하는 함수

minmax(최소,최대) 함수
- 최소 크기와 최대 크기를 정하는 함수

ex) grid-template-columns : repeat(3,minmax(200px,1fr));
- 각 열은 최소 200px이고 공간이 남는다면 1fr비율로 늘어난다.

auto-fit을 사용한 반응형 grid
- 화면 크기에 따라 카드 개수를 자동으로 바꾸고 싶을 때 사용한다.

ex) grid-template-columns : repeat(auto-pit,minmax(220px,1fr);
- 카드 하나의 최소 너비는 220px이고, 화면에 들어갈 수 있는만큼 자동 배치
- 남은 공간은 비율로 채우겠다.
- 화면이 넓으면 4개, 좁으면 2개, 모바일에서는 1개가 될 수 있다.

grid item의 위치 지정
- grid-column
- grid-row

grid-template-areas
- 화면 영역에 이름을 붙여 배치하는 방법

.layout{
	display:grid;
	grid-template-columns : 200px 1fr;
	grid-template-rows : 80px 1fr 60px;
	grid-template-areas :
		"a	a"
		"b	b"
		"c	c"
}

.item{
	grid-area : a;
}

부모 요소에 display : grid를 주고 시작한다.
가로칸은 grid-template-columns로 만든다.
세로칸은 grid-template-rows로 만든다
간격은 gap으로 조정한다.
같은 크기를 반복할 때는 repeat()를 사용한다.
반응형 카드 목록은 repeat(auto-fit,minmax())를 많이 사용한다.
웹페이지 전체구조는 grid-gemplate-areas를 사용하면 배치하기 쉽다

BEM 방법론
- CSS 클래스 이름을 일정한 규칙에 따라 작성하는 방법

HTML/CSS를 처음 작성할 때
<div class="box">
	<h2 class="title">상품명</h2>
	<p class="text">상품 설명</p>
	<button class="btn">구매하기</button>
</div>

.title{...}

.text{...}

.btn{...}

페이지가 커지게 되면 문제가 생길 수 있다.
- 클래스 이름이 너무 일반적이라서 다른 영역에서도 사용하면 스타일이 충돌할 수 있다.

block
독립적으로 존재할 수 있는 큰 덩어리
- header, nav, card, product-card, login-form, profile, footer ...

Element
Block 안에 포함되는 구성 요소
혼자 존재하기보다는 Block 안에서 의미가 있다.
보통 카드 안에는 이미지, 제목, 설명, 가격, 버튼이 있다.
card__image, card__title, card__description, card__price, card__button 

Modifer
Block이나 Element의 상태 또는 변형을 의미한다.
상품 카드 중에서 할인 상품을 강조하고 싶다면
<article class= "product-card product-card--sale:>

BEM을 쓰면 좋은 상황
- 페이지가 커질 때
- 카드, 버튼, 폼 같은 컴포넌트가 반복될 때
- 여러 사람이 같이 CSS를 작성할 때
- 클래스 이름 충돌을 줄이고 싶을 때

=====================================
06/24

다중 선택자
클래스를 여러개 지정을 한다.
<p class="a b c"> 내용 </p>

CSS에서 멀티클래스 선택하기

EX) a,b,c 클래스를 모두 가지고 있는 태그를 선택하기
.a.b.c {...}

<div class="a">
	<div class="b>
		<div class= "c">
		</div>
	</div>
</div>

.a .c

자식 선택자
- 바로 아래 자식만 선택
<nav>
	<a>메뉴1</a>
	<div>
		<a>메뉴2</a>
	</div>
</nav>

nav > a

인접 형제 선택자

<h1>제목</h1>
<p>문단1<p>
<p>문단2<p>

h1+p {...}
- 첫번째 p만 선택됨

일반 형제 선택자
뒤에 있는 형제 모두를 선택한다.

<h1>제목</h1>
<p>문단1<p>
<p>문단2<p>

h1 ~p {...}
-모든 p 선택

.a .c

자손선택자
<div class="a">
	<div class="b">
		<div class="c">
		</div>
	</div>
	
</div>

속성 선택자
특정 속성을 가진 요소를 선택

<input type="text" placeholde="이름입력">

input[type] {...} : input 태그 중에서 type 속성을 가진 태그를 선택

input[type="text"] {...} : input 태그 중에서 type="text"를 가진 태그를 선택

동적 의사 클래스

:hover
커서를 올렸을때
button:hover {...}

:active
클릭하는 순간
button:active {...}

:focus
입력창을 선택했을 때
input:focus {...}

:checked
체크박스가 선택됐을 때
input:checked {...}

:disabled
비활성화 상태일 때
input:disabled {...}

:enabled
활성화 상태일 때
input:enabled {...}

구조 의사 클래스
- 위치에 따라 선택

:first-child
첫번쨰 자식을 선택
li:first-child {...}

:last-child
마지막 자식을 선택
li:last-child {...}

:nth-child(n)
n번째 자식을 선택
li:nth-child(3) {...}
li:nth-child(even), li:nth-child(odd)

부정선택자
:not
제외하기

p:not(선택자)

.board > .post.notice p:first-child:hover {...}

BEM 방법론
클래스 이름을 너무 일반적으로 작성하면 겹칠 수 있으니 역할을 구체적으로 지정해서 식별하는 것

Block : 독립적으로 존재할 수 있는 큰 덩어리
header, nav, card, product-card, login-form, profile, footer

Element : Block안에 포함되는 구성요소
- Element는 혼자 존재하기보단 Block 안에서 의미가 있다.

Modifier : Block이나 Element의 상태 또는 변형을 의미한다.
ex) product-card product-card--sale

<article class="product-card">
	<div class="product-card__image"> 상품 이미지 </div>
	<h2 class="product-card__title"> 무선 마우스 </h2>
	<p class="product-card_desc"> 조용한 사무용 마우스 </p>
	<p class="product-card_price"> 29,000 </p>
	<button class="product-card__btn"> 구매하기 </button>
</article>

======================================================
06/25

반응형 웹
- 하나의 html 구조로 다양한 화면 크기를 지원한다.

픽셀 대신 비율(%), vw,vh,em,rem같은 단위를 사용한다.

max-width : 100%, height: auto;로 이미지가 부모 요소 너비에 맞게 줄어들게 한다.

미디어쿼리를 활용하여 특정 화면 크기 이상 또는 이하에서만 스타일을 적용한다.

모바일 퍼스트 원칙
- 모바일에서 먼저 디자인하고, 큰 화면으로 확장한다.
- CSS를 작성할 때 모바일 기준으로 기본 스타일을 먼저 작성, 화면이 커질때만 스타일을 추가한다.

@media(조건){
	스타일
}

너비가 최소 768px 이상이 되면 스타일을 적용
@media(min-width:768px){
	스타일
}

너비가 최소 1200px 이상이 되면 스타일을 적용
@media(min-width:1200px){
	스타일
}