07/20

실제 서비스는 많은 사람들이 로그인을 해서 사용하기 때문에 인증 매커니즘을 구현을 해야한다.

인증
- 사용자가 누구인지 확인하는 과정
- 사용자 아이디와 비밀번호
- 다중요소 인증(sms, email)
- 생체인증(지문, 안면 인식)
- 소셜 로그인(페이스북, 구글, 네이버, 카카오)

인가
- 사용자가 인증된 후에, 그 사용자가 무엇을 할 수 있는지 결정하는 과정
- 특정 자원이나 기능에 접근할 수 있는 권한을 부여하는 절차

Basic 인증
- Todo 프로그램은 로그인을 제외하면 상태를 유지해야할 이유가 없다.
- 상태가 없는 웹 애플리케이션에서 인증을 구현하는 방법은 모든 HTTP 요청에 아이디와 비밀번호를 같이 보내는 것이다.
- 아이디와 비밀번호를 Base64 인코딩하여 HTTP 요청 헤더에 포함한 뒤 요청을 보낸다.
- 서버는 데이터를 받아서 디코딩하여 사용자가 인증할 수 있는지 확인하고, 자격이 유효하면 요청한 자원에 접근을 허용한다.

토큰 기반 인증
사용자가 인증에 성공한 후 토큰을 발급받아, 이를 사용해 서버와의 통신에서 인증을 처리하는 방식
- 이 방식은 세션 기반 인증과는 달리, 서버에 사용자의 상태를 저장하지 않고, 토큰을 통해 인증을 관리하기 때문에 무상태 방식으로 작동한다.
- RESTful API와 같은 분산 시스템에서 많이 사용된다.

토큰
- 사용자를 구분할 수 있는 문자열
- 서버가 자기만의 규칙으로 토큰을 만들어준다.

서버가 사용자 인증
- id랑 비밀번호를 확인하여 사용자가 올바른 자격을 가지고 있는지 확인

토큰 발급
- 인증에 성공하면, 서버는 사용자의 정보 또는 권한을 포함한 토큰을 생성하여 클라이언트에게 반환한다.
- 이 토큰은 주로 JWT(JSON Web Token) 형식으로 사용한다.

클라이언트가 토큰을 저장한다
- 브라우저에 저장을 한다.

토큰을 포함해 요청을 보낸다.
- 클라이언트는 서버에 요청을 보낼때마다 이 토큰을 HTTP 요청 헤더에 포함시켜 인증을 처리한다.

서버가 토큰을 검증
- 서버는 클라이언트로부터 전달받은 토큰이 유효한지 확인한다.
- 토큰이 유효하다면, 서버는 요청을 처리하고 필요한 자원에 접근을 허용한다.

응답 반환
- 서버는 클라이언트에게 요청된 데이터를 반환하거나, 필요한 처리를 수행한다.

JWT
- JSON 형식으로 사용자의 정보를 저장하고 서명을 통해 토큰의 무결성을 검증하는 토큰 기반 인증 방식

JWT는 세 부분으로 나뉜다.
1. 헤더
2. 페이로드
3. 서명
이 세 부분을 Base64 URL 인코딩으로 인코딩한 후 마침표로 구분해 하나의 문자열로 합쳐진 것이 JWT이다.

서명(시그니쳐)헤더와 페이로드 Base64로 인코딩한 다음 비밀키와 함께 선택한 알고리즘을 사용해 만든 값
ㄴ 시그니쳐는 토큰이 위변조 되지 않았는지 검증하는 역할을 한다.

시그니쳐가 중요한 이유는, 클라이언트가 JWT를 서버에 보낼 때 서버는 시그니처를 다시 계산하고, 클라이언트가 보낸 시그니처와 비교해서
토큰이 변조되었는지 확인할 수 있기 때문이다

==============================================
07/21

Todo CRUD
표현계층 : 요청을 받고, 응답을 내보내는 것
ㄴ @GetMapping, @PostMapping, @PutMapping, @DeleteMapping

요청하는 쪽
fetch("주소",{옵션 get,post,put,delete});
비즈니스 계층 : 실제 로직을 처리
ㄴ 유효성 검사
ㄴ 실제 기능 처리
ㄴ CRUD

영속계층 : 데이터베이스에 CRUD
ㄴ ORM : 객체와 테이블을 1대1로 매핑하는 프레임워크
Spring data JPA : 메서드로 쿼리문을 처리할 수 있다.
기본적으로 CRUD 메서드를 제공한다
findAll() : 전체 조회
findById() : 아이디를 통한 조회 where id = ?
save() : 추가, 수정을 같이 쓴다.
delete() : 삭제(엔티티를 통한 삭제)

동작 By 필드명 조건

동장
find : 조회
count : 개수
exists : 존재하는지 확인
delete : 삭제

조건 
문자열 조건
Containing : 포함하는지
StartingWith : 특정 문자열로 시작
EndingWith : 특정 문자열로 끝나는

findByTitleContaining(String title)

비교조건(숫자, 날짜)
GreaterThan : 초과
GreaterThenEqual : 이상
LessThan : 미만
LessThanEqual : 이하
Between : 범위

가격이 30000원보다 큰 책을 찾는 쿼리메서드
findByPriceGreaterThan(int price)

부정조건
- 특정 값이 아닌 데이터를 검색
Not
NotContaining

특정 카테고리를 포함하지 않는 데이터를 조회
findByCategoryNot(String category)

Null조건
IsNull : Null인지 확인
IsNotNull : Null이 아닌지 확인

저자가 Null 아닌 데이터만 조회

여러 값 중 하나
In : 목록에 포함
Notln : 목록에 포함되지 않음

특정 카테고리를 포함하는 데이터만 조회
findByCategory(List<String> categories) -> SELECT * FROM BOOKS WHERE CATEGORY IN('프로그래밍','요리','추리','에세이')

Boolean 조건 
True : true값이 들어있는 데이터를 찾음
False : False값이 들어있는 데이터를 찾는다.

여러 조건의 연결
And : 그리고
Or : 또는

특정 카테고리와 특정 가격보다 높은 책 찾기
findByCategoryAndPriceGreaterThan(String category, int price)

정렬조건
OrderBy필드 : 정렬 시작
Asc : 오름차순
Desc : 내림차순

특정카테고리를 가지는 가격을 기준으로 오름차순하여 조회
findByCategoryOrderByPriceAsc(String category);

중복제거
Distinct

findByAuthorDistinct(String Author)

회원 기능 만들기
인증
- 사용자가 누구인지 확인하는 과정
인가
- 인증 후 사용자가 무엇을 할 수 있는지 결정하는 과정

아이디와 비밀번호를 사용한 Basic 인증

HTTP의 특성
무상태(stateless) : 이전 통신을 기억하지 않는다.
- 매 요청마다 인증정보를 함께 전달해야한다.
- 개인정보가 유출될 수 있다.
회원가입
로그인

토큰 기반 인증
- 사용자가 인증에 성공한 후 토큰을 발급받아, 서버와의 통신에서 인증을 처리하는 방식
- 토큰은 클라이언트의 브라우저에 저장이 된다. (자동로그인, 아이디 기억)

JWT(JSON Web Token)
- JSON 형식으로 사용자 정보를 저장하고 서명을 통해 토큰의 무결성을 검증하는 방식

구조
헤더 : 해싱 알고리즘, 토큰의 타입
페이로드 : 사용자 또는 시스템 관련 정보
서명 : 헤더와 페이로드를 Base64로 인코딩한 뒤 비밀키로 암호화한 문자열

JWT 토큰 생성 및 반환 구현
- 유저 정보를 바탕으로 헤더와 페이로드를 작성하고 서명한 뒤 토큰을 반환

JJWT 라이브러리
- JWT 토큰을 생성하고, 서명하고, 파싱하고, 검증해주는 기능을 가지고 있다.
JJWT-API : 개발자가 사용하는 JWT API 제공
JJWT-impl : JWT 기능의 실제 내부 구현체
JJWT-GSON : JWT의 JSON 직렬화/역직렬화를 Gson으로 처리

토큰을 발급해주는 시점
- 로그인을 할때 같이 발급

스프링 시큐리티 프레임워크
- 스프링 기반의 어플리케잇녀에서 인증과 인가를 처리하기 위한 강력한 보안 프레임워크

필터 기반 아키텍쳐
- 요청이 컨트롤러에 도착하기 전에 여러 보안 검색대를 통과하도록 만든 구조
- Todo에 대한 CRUD 작업을 할 때 검사를 진행한다.
- 로그인을 한 사람인가?
- JWT를 가지고 있는가
- 토큰이 유효한가?
- 만료되지는 않았는가?
이런 검사를 컨트롤러 앞에서 처리하는 역할을 Filter가 담당할 수 있다.
스프링 시큐리티는 여러개의 필터를 순서대로 연결하여 사용할 수 있다.
연결된 필터를 필터체인이라고 부른다.

OncePerRequestFilter : HTTP 요청 1번당 필터 로직이 한 번만 실행되도록 도와주는 Spring 필터 기반 클래스

doFilterInternal() : HTTP 요청이 들어오면 이 필터가 실행이 되고, 커스텀 필터를 만들 때 필터 처리 로직을 작성하는 메서드

HttpServletRequest request : 클라이언트가 보낸 HTTP 요청 정보
HttpServletResponse response : 클라이언트에게 보낼 HTTP 응답 정보
FilterChain filterChain : 다음 필터로 요청을 전달하는 객체

클라이언트 쪽에서 fetch()로 요청을 보낼떄 header에 토큰을 실어서 보낸다.
fetch("요청주소".{
	method : "GET",
	headers : {
		"Content-Type" : "application/json",
		"Authorization:"Bearer" + 토큰
	}
});

StringUtils.hasText(bearerToken) : 문자열 유효성 검증 메서드
- null인지 확인
- "' 빈 문자열인지 확인
- 공백만 있는지 확인
- 실제 문자열이 있는지 확인

1. 아이디와 비밀번호를 입력하고 로그인
2. 토큰을 발급받는다
3. 할일에 대한 추가/수정/조회/삭제 기능 요청(요청과 함께 토큰을 같이 보낸다.)
4. 필터에서 검증(아이디를 뽑아온다.)
5. 컨트롤러의 매개변수에 넣는다.
6. DB에 전달해 비교해서 응답을 만들어낸다.

hello@world.com을 이용해 할일 추가하기
hello2@world.com을 이용해 할일 추가
조회했을때 각자 추가한 할일만 보이는지 확인

maven, mybatis -> 학습 모듈 내용
gradle,jpa -> 요즘 트렌드

====================================
07/22

필터(filter) 사용자의 요청이 컨트롤러로 들어오기 전에 검증을 한다

JWT(JSON WEB TOKEN)
- 로그인한 사용자의 인증 정보를 문자열 형태의 토큰으로 만들어서 클라이언트의 브라우저에 저장

헤더 : 해싱 알고리즘, 타입
페이로드 : 사용자 정보, 발행 주체
서명 : 헤더와 페이로드를 비밀키와 함께 해싱한 문자열

로그인할 때 토큰을 발급

무상태(stateless)
- 서버가 이전 요청에서 사용자가 누구였는지 기억하지 않는 것

회원가입 + 로그인

인증

할 일(todo)에 대해 추가/수정/조회/삭제를 할때 유저마다 달라야 한다.
매 요청마다 사용자 정보가 들어있는 토큰을 같이 보내서 사용한다.

스프링 시큐리티를 사용해서 특정경로에 필터를 다 걸었다.

공통 모듈 구현
- 시스템을 만들 때 자주 사용하는 기능들로서 재사용이 가능하게 패키지로 제공하는 독립된 모듈
1. 재사용(Reuse)
목표로 하는 시스템의 개발 시간 및 비용 절감을 위하여 검증된 기능을 파악하고 재구성하여 시스템에 응용하기 위한 최적화 작업
- 함수와 객체 재사용 : 클래스나 함수 단위로 구현한 소스코드의 재사용
- 컴포넌트 재사용 : 컴포넌트 단위로 재사용하며, 컴포넌트의 인터페이스를 통해 통신한다.
- 어플리케이션 재사용 : 공통된 기능을 제공하도록 구현된 어플리케이션과의 통신으로 기능을 공유하여 재사용한다.

2. 공통 모듈의 종류
- 목표 시스템을 구축하기 위해 구축하는 공통 모듈들은 공통 모듈의 설계를 기반으로 프로그래밍 언어와 도구를 활용하여 작성한다.

공통기술
사용자 디렉터리/통합 인증 : 사용자 통합 인증, 로그인
보안 : 실명 확인, 역할 권한 관리, 암호화/복호화
통계/리포팅 : 통계
협업 : 게시판, 동호회, 일정 관리, 전자 우편, 주소록/명함, 문자, 전자 결제
사용자 지원 : 사용자 관리, 약관 관리, 개인화
시스템 관리 : 공통 코드 관리, 메뉴 관리, 프로그램 관리, 로그 관리, 배치 관리
시스템/서비스 연계 : 시스템 연계
디지털 자산 관리 : 지식 관리

모바일 공통 기술
협업 : 모바일 신시간 공지, 오프라인 웹 서비스, 모바일 위치 연계 서비스. 모바일 기기 식별, 모바일 메뉴
사용자 지원 : 모바일 메뉴 관리
시스템/서비스 연계 : 시스템 연계
디지털 자산 관리 : 모바일 차트/그래프, 모바일 사진 앨범

요소 기술
달력, 웹 에디터, 인쇄/출력, 쿠키/세션, 포맷/계산/변환, 메시지 처리, 인터페이스 화면, 시스템, 파일 업로드/다운로드, 이중 등록 방지

★★★★★★★★★★
소프트웨어 모듈 응집도
- 응집도는 모듈 내부에서 구성 요소 간에 밀접한 관계를 맺고 있는 정도로 평가되며, 응집도가 높을수록 필요한 요소들로 구성되어있고
낮을수록 관련이 적은 요소들로 구성되어 있다.

기능적 응집도(Functional Cohension)
- 모듈 내부의 모든 기능이 단일한 목적을 위해 수행되는 경우
- 구조도 최하위 모듈에서 많이 발견된다.

순차적 응집도(Sequential Cohension)
- 모듈 내에서 한 활동으로부터 나온 출력값을 다른 활동의 입력값으로 사용하는 경우

통신적 응집도(Communication Cohension)
- 동일한 입력과 출력을 사용하여 다른 기능을 수행하는 활동들이 모여있을 경우

절차적 응집도(Procedual Cohension)
- 모듈이 다수의 관련 기능을 가질 때 모듈 안의 구성 요소들이 그 기능을 순차적으로 수행할 경우

시간적 응집도(Temporal Cohension)
- 연관된 기능이라기보단 특정 시간에 처리되어야 하는 활동들을 한 모듈에서 처리하는 기능

논리적 응집도(Logical Cohension)
- 유사한 성격을 갖거나 특정형태로 분류되는 처리 요소들이 한 모듈에서 처리되는 경우

우연적 응집도(Coincidental Cohension)
- 모듈 내부의 각 구성 요소들이 연관이 없을 경우

응집도와 품질
응집도가 높을수록 높은 품질, 낮을수록 낮은 품질이다.
1. 기능적 응집도
2. 순차적 응집도
3. 통신적 응집도
4. 절차적 응집도
5. 시간적 응집도
6. 논리적 응집도
7. 우연적 응집도
(1 -> 7로 내려갈 수록 품질이 낮아진다.)

소프트웨어 결합도
- 모듈과 모듈간의 관련성 정도, 관련이 적을수록 모듈의 독립성이 높아져 모듈 간 영향이 적어지게 된다.

스탬프결합도(Stamp Coupling)
- 모듈 간의 인터페이스로 모듈이나 오브젝트, 스트럭쳐 등이 전달되는 경우

제어 결합도(Control Coupling)
- 단순 처리할 대상인 값만 전달되는게 아니라 어떻게 처리해야 한다는 제어요소가 전달되는 경우

외부 결합도(External Coupling)
- 다수의 모듈이 모듈 밖에서 도입된 데이터, 프로토콜, 인터페이스 등을 공유할 때 발생하는 경우

공통 결합도(Common Coupling)
- 파라미터가 아닌 모듈 밖에 선언되어 있는 전역 변수를 참조하고 전역 변수를 갱신하는 식으로 상호작용하는 경우

내용 결합도(Content Coupling)
- 다른 모듈 내부에 있는 변수나 기능을 다른 모듈에서 사용하는 경우이다.

결합도와 품질
- 목표시스템의 환경에 따라 모듈 구성의 결합 정도를 결정할 수 있으나, 품질 측면에서 결합도의 유형 중 자료 결합도의 품질이 가장 높고,
내용 결합도의 품질이 가장 낮다.

공통 모듈 구현
공통 모듈의 상세 설계를 기반으로 작성해야 할 공통 모듈을 확인한다.
1. 공통 모듈을 구현하기 위한 오브젝트(DTO/VO)를 정의한다.
VO(ValueObject) : 값 자체를 표현하기 위한 객체

2. 공통 모듈을 구현하기 위한 SQL을 작성한다.
- 공통 코드 조회를 위한 SQL 쿼리문을 마이바티스 XML 파일로 구현한다

3. 데이터 접근 객체(DAO : Data Access Object)를 구현한다.
- DAO에서는 SQL문을 구현한 XML 파일에서의 해당 ID값을 호출하여 데이터를 조회하거나 조작한다.

4. 서비스(Service)클래스를 구현한다.
- 동일한 시스템 내에서 사용되는 결합도가 낮은 공통 모듈을 구현하기 위해 스탬프 결합도 수준의 서비스를 구현한다.

5. 컨트롤러(Controller) 클래스를 구현한다.

/////////////

Maven
- Java 프로젝트의 빌드와 의존성을 관리해주는 도구
1. 필요한 라이브러리를 다운로드
2. Java 코드 컴파일
3. 테스트 코드를 실행
4. 프로젝트를 빌드
5. JAR 또는 WAR 파일 생성
6. 프로젝트 실행에 필요한 의존성 관리

pom(Project Object Model)
- Maven이 프로젝트를 어떻게 관리해야 하는지 설정하는 파일
pom.xml

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

dtd(Document Type Definition)
xml 문서가 어떤 태그를 사용할 수 있고 어떤 구조로 작성되어야 하는지 정의하는 규칙

mybatis의 dtd
<mapper>
<select>
<insert>
<update>
<delete>
<resultMap>
<sql>과 같은 태그를 사용할 수 있다.

gradle vs maven

jpa vs mybatis

