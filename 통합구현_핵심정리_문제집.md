# 통합구현(연계) 핵심 개념 정리 & 서술형 문제집

---

## PART 1. 핵심 개념 정리

### 1) 연계 요구사항 분석

**연계 데이터 식별(Identification)**
서로 다른 시스템이 데이터를 주고받기 위해, 연계 대상이 되는 데이터를 찾아내고 동일한 기준으로 정의하는 과정. 이 과정이 부실하면 시스템 간 데이터·코드 체계 불일치, 데이터 중복·누락이 발생하고 유지보수성이 크게 떨어진다.

**분석 절차 4단계 (순서 암기 포인트: 파악→수집→분석→정리)**
1. 현행 시스템 파악
2. 사용자 요구사항 수집
3. 연계 데이터 분석
4. 결과 정리(산출물 작성)

**분석 시 활용되는 입력물**
- 시스템 구성도: H/W, S/W, 네트워크 연결 구조 → 통신·환경적 제약사항 사전 확인
- ERD: 테이블/속성/엔티티 관계 파악 → 연계 대상 테이블과 추출·저장 항목 정확히 식별

**최종 산출물 3종**
- 요구사항 분석서 (요구사항 ID, 명, 유형, 상세설명, 제약·종료조건)
- 인터페이스(연계) 정의서 (송수신 데이터 항목, 형식, 전송방식, 연계 주기)
- 회의록 (일시, 참석자, 논의내용, 합의사항, 향후일정)

**연계 정의서 핵심 속성 5가지**
1. 송신 시스템
2. 수신 시스템
3. 인터페이스 테이블/파일
4. 연계 서버(어댑터)
5. 로깅 및 모니터링 항목

---

### 2) 연계 방식 (직접 vs 간접)

| 구분 | 직접 연계 (Point-to-Point, DB Link) | 간접 연계 (EAI/ESB) |
|---|---|---|
| 결합도 | 강함 (High Coupling) | 느슨함 (Loose Coupling) |
| 장애 영향 | 한 시스템 변경 시 연결된 모든 시스템 영향 | 연계 서버가 완충 역할 → 개별 영향 최소화 |
| 확장성 | 시스템 n개 증가 시 연계 수 기하급수적 증가 | 어댑터 설정만으로 손쉽게 확장 |
| 관리 | 개별 관리, 모니터링/장애제어 어려움 | 중앙집중식 통합 관리(라우팅, 인증, 암호화, 재처리) |

**연계 서버 도입 이점**: 프로토콜·데이터 형식 변환을 연계 서버가 전담 → 각 시스템은 본연의 업무 로직에만 집중 가능.

---

### 3) 데이터 표준화 및 매핑

- **매핑(Mapping)**: 서로 다른 두 시스템 간 연계 데이터 항목을 정합성에 맞춰 1:1, 1:N 등으로 대응·연결하는 과정
- **데이터 타입 불일치 문제**: 송신(문자열) ↔ 수신(Date 등) 타입이 다르면 Format Mismatch Error 발생 → 타입 일치 및 변환 로직 적용, 로그 확인 필수
- **공통 코드 매핑 정의서**
  - 목적: 데이터의 일관성과 상호운용성 확보
  - 원칙: ① 1:1 매핑의 원칙 ② 표준화 및 확장성의 원칙

---

### 4) 연계 처리 메커니즘

**전체 처리 흐름 (송신 2단계 + 수신 2단계)**
1. 송신 데이터 추출 및 변환
2. 송신 어댑터/로그 기록 및 전송
3. 수신 데이터 수신 및 검증
4. 수신 DB 반영 및 변환

**인터페이스 테이블(I/F Table)을 거치는 이유**
- 데이터 정합성: 업무 DB 스키마·규칙 준수 여부를 검증하는 버퍼 역할 (직접 적재 시 오염 위험)
- 오류 복구: 예외 발생 시에도 원본 데이터·로그가 보존되어 원인분석·재처리 용이

**2단계 커밋(2-Phase Commit)**: 준비(Prepare) 단계 → 전송(Commit) 단계로 동작하여 원자성(Atomicity) 보장

**오류 재처리(Retry) 절차**
전송 실패 감지 → 재시도 대기 및 카운트 → 재전송 수행 → 최종 오류 처리

---

### 5) 보안

**암호화 방식 비교**

| 구분 | 대칭키 | 비대칭키(공개키) |
|---|---|---|
| 키 | 암/복호화 키 동일 | 암호화(공개키)·복호화(개인키) 키 쌍이 다름 |
| 특징 | 키 전달·관리 어려움, 속도 빠름 | 키 관리 용이·안전, 연산 속도 느림 |
| 예시 | AES, SEED | RSA, ECC |

**전송구간 암호화(HTTPS/TLS)만으로는 부족한 이유**: DB 자체 데이터는 평문 저장 → DB 유출·내부자 유출 시 무방비 노출. → **DB 암호화 + 접근통제/키 별도 관리** 필요.

---

### 6) 인터페이스 설계 시 유의사항

- 민감정보(ID/PW 등)를 GET 방식 Query Parameter에 담으면 URL 로그·히스토리에 그대로 노출 → **POST + Request Body(JSON/Form)**로 전송
- 대용량 처리 시 Connection Timeout 예방: 비동기 처리·메시지 큐 도입, 배치·분할 전송

---

### 7) 오류 코드 체계 (예시 규칙: E + 위치1 + 유형1 + 일련번호3)

- 위치: A(연계 응용프로그램), S(서버) 등
- 유형: D(중복), M(필수누락), F(포맷오류) 등
- 예) `EAD001` = 연계 데이터 중복(기본키 충돌), `EAM001` = 필수 연계항목 누락

---

### 8) 테스트 케이스 명세서 필수 항목 5가지
1. 테스트 케이스 ID
2. 테스트 시나리오 및 목적
3. 사전 조건
4. 입력 데이터
5. 예상 결과

---

### 9) 로깅/추적성

- 분산환경에서 하나의 연계 건을 끝까지 추적하려면 **Trace ID(고유 추적ID)** 발급 → 송신~연계서버~수신까지 유지, 단계별 타임스탬프·처리상태 기록
- SLA 검증: '송신 시작시간' vs '수신 반영 완료시간' 차이 분석 → 병목구간 식별 근거로 활용

---

### 10) 연계 방식 선택 (실시간 vs 배치)

- 고려 요소: **데이터 적시성**(즉시성 필요 여부), **네트워크/DB 부하**(대용량 여부)
- 전량 실시간 처리 시 부작용: DB 트랜잭션·락 폭주 → 성능저하·메모리부족, 네트워크 과부하 → 타임아웃 빈발 → 서비스 전체 장애 위험

### 11) 동기식 vs 비동기(메시지 큐) 이벤트 처리

- 동기식: 모든 하위 시스템 처리 완료까지 대기 → 응답지연, 하나라도 장애 시 전체 트랜잭션 실패(연쇄 장애)
- 메시지 큐 기반 비동기: 즉시 응답 가능, 결합도 낮음, 장애 발생해도 메시지는 큐에 안전 보관 → 복구 후 순차 처리 → 가용성·결함허용성 확보

---
---

## PART 2. 서술형 연습 문제집

### 문제 1
연계 정의서에 반드시 포함되어야 하는 핵심 속성 요소 5가지를 쓰시오.

<details><summary>모범답안</summary>
① 송신 시스템 ② 수신 시스템 ③ 인터페이스 테이블/파일 ④ 연계 서버(어댑터) ⑤ 로깅 및 모니터링
</details>

---

### 문제 2
연계 요구사항 분석 절차 4단계를 순서대로 쓰시오.

<details><summary>모범답안</summary>
현행 시스템 파악 → 사용자 요구사항 수집 → 연계 데이터 분석 → 결과 정리
</details>

---

### 문제 3 [시나리오]
G 병원은 송신 DB의 환자 체중 데이터(문자열 "68.5kg")를 수신 DB의 숫자(Numeric) 컬럼으로 그대로 전송하도록 설계하였다. 연계 실행 시 수신 측에서 지속적으로 Type Conversion Error가 발생하고 있다.

문제 발생 원인과 올바른 표준화/변환 방안을 서술하시오.

<details><summary>모범답안</summary>
원인: 송신측 문자열 데이터에 단위(kg)가 포함되어 있어 수신측 숫자 타입으로 직접 변환이 불가능함.
해결: 송신 측 또는 중계 시스템에서 단위를 제거하고 숫자형으로 변환하는 표준화 로직을 적용하며, 변환 후 정상 처리 여부를 로그로 확인한다.
</details>

---

### 문제 4
공통 코드 매핑 정의서의 목적과 작성 시 고려해야 할 기본 원칙 2가지를 서술하시오.

<details><summary>모범답안</summary>
목적: 데이터의 일관성과 상호운용성 확보
원칙: ① 1:1 매핑의 원칙 ② 표준화 및 확장성의 원칙
</details>

---

### 문제 5
연계 메커니즘의 주요 처리 단계 4가지(송신 2단계, 수신 2단계)를 순서대로 쓰시오.

<details><summary>모범답안</summary>
① 송신 데이터 추출 및 변환 ② 송신 어댑터/로그 기록 및 전송 ③ 수신 데이터 수신 및 검증 ④ 수신 DB 반영 및 변환
</details>

---

### 문제 6 [시나리오]
P 그룹은 계열사 20여 개 시스템을 모두 DB Link 방식으로 직접 연계하였다. 한 계열사의 DB 계정정보가 변경되자 연쇄적으로 여러 시스템이 동시에 장애를 일으켰다.

기술적 원인과 해결을 위한 미들웨어 방식을 제시하시오.

<details><summary>모범답안</summary>
원인: 직접 연계 방식은 시스템 간 결합도가 강하여, 한 시스템의 변경이 연결된 모든 시스템에 영향을 미침.
해결: EAI/ESB 기반의 간접 연계 방식을 도입하여 결합도를 낮추고 중앙에서 통합 관리한다.
</details>

---

### 문제 7
2단계 커밋(2-Phase Commit)의 동작 방식과, 오류 재처리(Retry) 메커니즘의 동작 절차를 각각 서술하시오.

<details><summary>모범답안</summary>
2PC: 준비(Prepare) 단계 → 전송(Commit) 단계로 동작하여 원자성을 보장한다.
Retry: 전송 실패 감지 → 재시도 대기 및 카운트 → 재전송 수행 → 최종 오류 처리
</details>

---

### 문제 8 [시나리오]
Q 물류센터는 연계 응용 프로그램(A)을 통해 입고 데이터를 수신하는데, 필수 항목인 상품코드가 누락된 채 전송되어 처리가 실패하였다.

지정 규칙(E+위치1+유형1+일련번호3)에 따라 필수입력 오류(M)에 해당하는 6자리 코드와 메시지를 작성하시오.

<details><summary>모범답안</summary>
EAM001 : 필수 연계 항목 누락 - 상품코드 값이 Null로 수신됨
</details>

---

### 문제 9
대칭키와 비대칭키 암호화 방식의 키(Key) 관점 차이를 설명하고, 대칭키 알고리즘 예시 2가지를 쓰시오.

<details><summary>모범답안</summary>
대칭키는 암/복호화에 동일한 키를 사용하며 키 전달·관리는 어렵지만 속도가 빠르다. 비대칭키는 암호화용 공개키와 복호화용 개인키가 쌍으로 존재하여 키 관리는 용이·안전하지만 연산 속도가 느리다.
예시: AES, SEED
</details>

---

### 문제 10 [시나리오]
R 기업은 개인정보를 연계하며 HTTPS 전송구간 암호화만 적용했는데, 해킹으로 수신측 DB가 유출되었을 때 저장된 개인정보가 평문으로 노출되었다.

원인과 추가 보안 대책을 서술하시오.

<details><summary>모범답안</summary>
원인: 전송구간만 암호화되어 있어 DB 내부에는 평문으로 저장되므로, DB 유출·내부자 유출 시 데이터 보호가 불가능함.
대책: DB 자체 암호화 적용, 접근 통제 및 암호화 키의 별도 보안 관리
</details>

---

### 문제 11 [시나리오]
S 서비스는 REST API 연계 시 사용자 로그인 정보를 GET 방식 URL Query Parameter(?id=...&pw=...)로 전송하도록 구현하였다.

문제점 2가지와 올바른 수정 방식을 서술하시오.

<details><summary>모범답안</summary>
문제점: ① 민감정보가 URL에 그대로 노출되어 로그·히스토리에 남음 ② GET 방식은 인증정보 전송에 부적합한 HTTP Method임.
수정안: POST 방식으로 변경하여 HTTP Request Body에 JSON 또는 Form 데이터로 전송한다.
</details>

---

### 문제 12
대용량 연계 처리 시 발생할 수 있는 Connection Timeout 장애를 예방하기 위한 개발 측면 해결 방안 2가지를 서술하시오.

<details><summary>모범답안</summary>
① 비동기 처리 및 메시지 큐 도입 ② 배치 처리 및 분할 전송
</details>

---

### 문제 13
테스트 케이스 명세서에 반드시 포함되어야 하는 항목 5가지를 쓰시오.

<details><summary>모범답안</summary>
① 테스트 케이스 ID ② 테스트 시나리오 및 목적 ③ 사전 조건 ④ 입력 데이터 ⑤ 예상 결과
</details>

---

### 문제 14 [시나리오]
T 시스템 통합테스트 중, 동일 주문번호에 대한 배송 데이터가 중복 수신되어 PK 충돌 오류가 발생했다.

지정 규칙(E+위치1+유형1+일련번호3)에 따라 중복(D) 오류의 6자리 코드와 메시지를 작성하시오.

<details><summary>모범답안</summary>
EAD001 : 연계 데이터 중복 - 기본키 충돌로 인한 수신 실패
</details>

---

### 문제 15 [시나리오]
U 개발자는 연계 모듈에서 예외처리 구문 없이 쿼리 실행 코드만 작성하였고, DB 타임아웃 발생 시 프로그램이 아무 로그 없이 비정상 종료되었다.

문제 원인과 올바른 예외처리·로깅 구현 방안을 서술하시오.

<details><summary>모범답안</summary>
원인: try-catch 등 예외처리 구문이 없어 예외 발생 시 원인을 남기지 못하고 프로그램이 비정상 종료됨.
방안: try-catch 구문으로 예외를 처리하고, 표준 로깅 프레임워크를 통해 발생시각·인터페이스 ID·에러메시지·스택트레이스를 로그 DB에 기록한다.
</details>

---

### 문제 16 (서술형·심화)
'연계 데이터 식별'의 개념을 서술하고, 이 과정이 제대로 이루어지지 않았을 때 발생하는 문제점과 중요성을 설명하시오.

<details><summary>모범답안</summary>
개념: 서로 다른 시스템이 데이터를 주고받기 위해 필요한 데이터를 찾아내고 동일한 기준으로 정의하는 과정.
문제점/중요성: 식별이 부실하면 시스템 간 데이터·코드 체계 불일치, 데이터 중복·누락이 발생할 수 있음. 데이터 정합성 보장과 개발 생산성·유지보수성 향상을 위해 반드시 필요한 과정임.
</details>

---

### 문제 17 (서술형·심화)
인터페이스 테이블(I/F Table)을 거쳐 단계적으로 데이터를 반영하는 이유를 '데이터 정합성'과 '오류 복구' 관점에서 서술하시오.

<details><summary>모범답안</summary>
데이터 정합성: 업무 DB 스키마·규칙 준수 여부를 검증하는 중간 버퍼 역할을 하여, 검증 없이 직접 적재 시 발생할 수 있는 데이터 오염을 방지함.
오류 복구: 업무 DB 반영 중 예외가 발생해도 원본 데이터와 로그가 인터페이스 테이블에 보존되어 있어 원인분석과 재처리가 용이함.
</details>

---

### 문제 18 (서술형·심화)
동기식 API 호출 방식과 메시지 큐 기반 비동기 이벤트 처리 방식을 비교하여, 후자가 시스템 안정성 측면에서 갖는 효과를 설명하시오.

<details><summary>모범답안</summary>
동기식은 모든 하위 시스템 처리가 끝날 때까지 대기해야 하므로 응답속도가 느리고, 하나의 시스템이라도 장애가 나면 전체 트랜잭션이 실패하는 연쇄 장애가 발생함.
메시지 큐 기반 비동기 처리는 이벤트 발생 즉시 응답이 가능해 결합도가 낮아지고, 수신 시스템 장애 시에도 메시지가 큐에 안전하게 보관되었다가 복구 후 순차 처리되므로 데이터 손실 없이 가용성과 결함 허용성을 확보할 수 있음.
</details>

---

### 문제 19 (서술형·심화)
실시간 연동과 배치 연동 중 하나를 선택할 때 고려해야 할 핵심 요소 2가지를 설명하고, 모든 연계를 실시간으로 처리했을 때의 부작용을 서술하시오.

<details><summary>모범답안</summary>
고려요소: ① 데이터 적시성(즉시 처리 필요 여부) ② 네트워크·DB 부하(데이터 건수·크기)
부작용: 대용량 트랜잭션이 몰릴 경우 운영 DB에 락과 트랜잭션이 폭주하여 성능저하·메모리 부족이 발생하고, 네트워크 과부하로 타임아웃이 빈번해져 서비스 전체가 불능 상태에 빠질 수 있음.
</details>

---

### 문제 20 (서술형·심화)
분산 환경에서 하나의 연계 건을 끝까지 추적하기 위해 로그에 도입해야 하는 요소를 설명하고, 이 로그가 SLA 준수 검증에 어떻게 활용되는지 서술하시오.

<details><summary>모범답안</summary>
추적 요소: 고유 Trace ID를 발급하여 송신 → 연계서버 → 수신까지 전체 흐름에서 동일하게 유지하고, 단계별 타임스탬프와 처리상태를 함께 기록함.
SLA 활용: '송신 시작시간'과 '수신 DB 반영 완료시간'의 차이를 분석하여 구간별 지연시간을 측정하고, 목표 처리속도 충족 여부 검증 및 병목구간 파악·구조개선 근거로 활용함.
</details>

