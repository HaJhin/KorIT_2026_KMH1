# 07/27 시험 대비 암기 정리

---

## 1️⃣ 서버 프로그램 구현 (실습 파트)

**평가 범위**: Maven, MyBatis, VO, DAO, Mapper, Service, Controller, Postman

### 에러코드 암기 (디버깅 순서)
| 코드 | 의미 | 체크 포인트 |
|---|---|---|
| **400** | 요청이 잘못됨 | 요청 주소 / 전달 데이터 / Java 코드 로직 확인 |
| **404** | 경로를 못 찾음 | 요청 주소(URL) 확인 |
| **500** | 서버 내부 에러 | 구조 점검 (Controller → Mapper 순서로) |

> 👉 **암기 팁**: "4XX는 내(클라이언트) 탓, 500은 서버(내부) 탓"

### 프로젝트 세팅 순서
1. 프로젝트 생성 (의존성: **springweb, lombok, mybatis, mysqlconnector, devtools**)
2. DB 생성 → 테이블 생성
3. 프로젝트 import
4. 구조 구성: **VO → DAO → Service → Controller → Mapper**

---

## 2️⃣ 응집도 & 결합도

- **응집도는 강하게, 결합도는 약하게** ← 좋은 설계의 대원칙

**응집도 (강한 순 → 약한 순, 7단계)**
> 기능적 - 순차적 - 교환적 - 절차적 - 시간적 - 논리적 - 우연적
> 👉 암기: **"기순교절시논우"** (기능이 순서대로 교환하며 절차, 시간, 논리, 우연으로 흩어짐)

**결합도 (약한 순 → 강한 순, 6단계)**
> 자료 - 스탬프 - 제어 - 외부 - 공통 - 내용
> 👉 암기: **"자스제외공내"**

---

## 3️⃣ SOLID 원칙 (5가지)

| 원칙 | 핵심 문장 | 한 줄 요약 |
|---|---|---|
| **S**RP (단일 책임) | 클래스는 하나의 기능만 | 응집도↑, 수정 시 부작용 방지 |
| **O**CP (개방-폐쇄) | 확장에는 열림, 수정에는 닫힘 | 기존 코드 안 건드리고 기능 추가 (인터페이스/추상클래스 활용) |
| **L**SP (리스코프 치환) | 자식은 부모를 대체 가능 | 다형성의 신뢰성 확보 |
| **I**SP (인터페이스 분리) | 안 쓰는 메서드에 의존 X | 불필요한 결합 제거 |
| **D**IP (의존 역전) | 구체 X, 추상에 의존 | 자료 결합도 수준으로 낮춤 |

> 👉 암기: **"단일 개방 리스코프 인터페이스 의존"** → SRP-OCP-LSP-ISP-DIP 순서 그대로 외우기

---

## 4️⃣ 시스템 연계 (A쇼핑몰 사례 기반)

### (1) 요구사항 분석 없이 개발 착수 시 문제점 3가지
1. 데이터 불일치
2. 시스템 간 미연결
3. 재개발 발생

### (2) 연계 데이터 필수 질문 4가지
> **범위 / 형식 / 주기·패턴 / 전송방식·보안**
> 👉 암기: **"범형주보"**

### (3) 연계 요구사항 분석 절차 4단계 (순서 중요!)
1. 현행 시스템·운영환경 파악
2. 사용자 요구사항 수집
3. 연계 데이터 분석
4. 결과 정리
> 👉 암기: **"현수분결"**

### 배치(Batch) 정의
대량 데이터를 **정해진 시간에 자동으로** 일괄 처리하는 프로그램

---

## 5️⃣ 컬럼/코드 불일치 문제 해결

### 컬럼명이 다를 때 (memberName ↔ customer_name)
- **데이터 매핑**: 서로 다른 항목을 1:1(또는 규격)로 연결 + 형식 일치

### 코드 값이 다를 때 (A/U ↔ ADMIN/USER)
- **사전 검토 이유**: 같은 의미로 작동하는지 확인, 코드 불일치로 인한 오류 방지
- **조치 방안**: 연계 과정에서 **자동 변환 규칙(매핑 테이블)** 정의

### 운영 환경(OS/DBMS/WAS)이 다를 때
- **사전 파악 목적**: 호환성 문제·통신 장애 등 **환경적 리스크 사전 예방**

### 데이터 잘림(Data Loss) 방지 - 컬럼 속성 확인 항목 2가지
1. **데이터 타입** — 상호 호환 여부 확인
2. **데이터 길이** — 수신 측이 송신 측보다 짧으면 잘림/저장오류 발생 → 충분한 길이 확보

---

## 6️⃣ 입력물(Input) 문서별 핵심 정보

| 문서 | 제공 정보 |
|---|---|
| **시스템 구성도** | HW/SW/네트워크 전체 연결 구조 |
| **응용 애플리케이션 구성** | 화면·프로세스 구조, 기능 메뉴, 연계 데이터 발생 시점/주기 |
| **테이블 정의서** | 컬럼명, 데이터 타입, 길이 비교 |
| **코드 정의서** | 공통 코드 체계 및 의미 |
| **ERD** | 테이블 구조 및 관계 |

> 👉 암기: **"시스템 구성도=구조 / 응용 구성=화면·시점 / 테이블 정의서=컬럼 / 코드 정의서=코드 / ERD=관계"**

---

## 7️⃣ 연계 데이터 식별 & 표준화

### 연계 데이터 식별이란?
서로 다른 시스템이 데이터를 주고받기 위해 **필요한 데이터를 찾아 동일 기준으로 정의**하는 과정

- **필요성**: 인터페이스 정의서 작성 및 개발 기준 마련, 데이터 불일치 오류 예방

### 표준화 안 하면 생기는 문제 2가지
1. 코드 값 불일치로 **연동 실패**
2. 처리 결과 상이 → **데이터 신뢰성/품질 저하**

### 데이터 길이 초과 문제 (memberName 30자↑ → customer_name VARCHAR(20))
- **문제**: 잘림 현상, DB 저장 트랜잭션 에러
- **해결**: 수신 측 컬럼 길이 확충 OR 송신/중계 단계에서 길이 검증·제한 규칙 적용

### 데이터 정제(Cleansing) 정책 - phone → tel
- 숫자만 입력된 전화번호에 **하이픈(-) 삽입**하여 표준 포맷으로 변환

---

## 8️⃣ 연계 표준화 – Input / Output 문서 총정리

**Input (분석용) 3가지**
- 테이블 정의서 → 컬럼명·타입·길이 비교
- 코드 정의서 → 공통 코드 체계 확인
- ERD → 테이블 구조·관계 파악

**Output (결과물) 2가지**
- **인터페이스 정의서**
- **연계 요구사항 분석서**

> 👉 암기: **"인풋은 테이블·코드·ERD (3개), 아웃풋은 인터페이스·요구사항 정의서 (2개)"**

---

## ⭐ 최종 압축 암기 카드

- 에러코드: 400=요청문제 / 404=경로없음 / 500=서버(구조)문제
- 응집도: **기순교절시논우** (강→약)
- 결합도: **자스제외공내** (약→강)
- SOLID: **단일-개방-리스코프-인터페이스-의존**
- 연계질문 4가지: **범형주보**
- 연계분석 절차: **현수분결**
- 연계 Input 3 / Output 2
