JavaScript
화면을 동적으로 움직일 수 있게 해주는 프로그래밍 언어

할 수 있는 것
화면의 조작 : HTML 요소를 선택하고, 읽고 바꾸기
이벤트 처리 : 클릭,입력,스크롤,호버 등이 발생했을 때 처리
데이터 처리 : 연산, 조건, 반복
서버와 통신 : API 요청으로 데이터 주고받기

JS로 요청할 때 
ajax -> 다양한 객체와 메서드에 대해서 알아야 함(복잡)
fetch() -> api 요청 내장 함수(상대적으로 간편)

html에서 js를 사용하는 방법
1. <script> ~ </script> 태그 내에서 작성
2. js 파일을 만들어서 작성 후 html 내부에서 import
<script src="파일의 경로"></script>
3. 태그 내부에서 이벤트 속성을 통해서 작성하기
<button onclick="눌릴 시 액션"></button>

- 현재 웹페이지 전체를 의미하는 객체이다.

<html>
	<head>
		<title>제목</title>
	</head>

	<body>
		<h1>안녕하세요<h1>
		<p>JS 공부중 </p>
	</body>
</html>

브라우저는 위 코드를 화면에만 그리는 것이 아니라 객체로도 만들어둔다.

document객체가 필요한 이유
- JavaScript는 HTML을 직접 수정할 수 없다
- 반드시 document를 통해 접근을 해야 한다.

<h1 id="title">안녕하세요</h1>

- CSS 선택자로 태그를 선택하는 방법도 있음

document 객체가 하는 일
- HTML 요소 찾기
- 글자 변경
- 이미지 변경
- CSS 변경
- 새로운 태그 생성
- 태그 삭제
등을 할 수 있다.

document의 대표적인 메서드
document.write() -> 문서에 글자를 출력한다.
※ body에 있는 내용은 지우고 출력을 하기 때문에 실무에서 많이 쓰지 않는다.

document.createElement() -> 새로운 태그를 만든다.(만드는 역할만 한다.)

document.body -> 웹페이지의 body에 접근을 한다.

body.appendChild(요소); -> 부모 요소의 마지막 자식으로 새로운 요소를 추가하겠다.

꼭 body가 아니라 내가 js로 선택한 태그의 자식으로도 추가할 수 있다.

자료형
내가 넣으려는 데이터의 타입

number 	숫자형
string 	문자형
boolean 	논리형
undefined	변수는 선언했으나 값을 초기화하지 않았을 때
null		의도적으로 값이 들어있지 않다고 선언해줄 때
bingint	큰 숫자를 넣을 때 (숫자 뒤에 n이 붙는다.)
symbol	겹치지 않는 고유한 값을 만들고 싶을 때

변수(Variable)
변수선언을 할 때 var, let, const와 같은 키워드를 사용한다.

var : 예전에 사용하던 변수 선언 키워드
- 같은 변수명으로 재선언도 가능하다.
- 대입이 가능하다.
- 같은 이름의 변수를 실수로 여러번 만들어도 오류가 발생하지 않아서 버그가 생기기 쉽다.

let : 값이 변경될 수 있는 변수를 선언하는 키워드
let 변수명 = 값;
- 대입은 가능하다.
- 같은 변수명으로 재선언하는 것은 불가능하다.

const : 값이 변경되지 않는 상수를 선언하는 키워드
- 상수를 선언할 때 값을 초기화해야 한다.
- 대입을 할 수 없다
- 같은 이름으로 재선언을 할 수 없다.
- 변하면 안되는 값을 저장할 때 사용한다.

ECMAScript
- JavaScript가 따라야 하는 표준
- ES6 (ECMAScript 2015)

연산자
산술연산자
대입연산자(복합대입연산자의 사용법)
비교연산자
ㄴ 느슨한 비교 (==) : 타입이 달라도 값만 같으면 인정
ㄴ 엄격한 비교 (===) : 타입과 값이 모두 같아야 인정
논리연산자(falsy,truthy값이 왔을 때의 연산법)
증감연산자(선행 증감,후행 증감)
삼항연산자

형변환

명시적 형변환
함수를 이용한 형변환
java에는 메서드만 존재한다. -> 독립적으로 호출 불가능
js는 독립적으로 호출 가능한 함수가 존재한다.
document.write() -> write()는 메서드
Number() -> 독립적으로 호출하니까 함수

Number() -> 정수로 형변환
String() -> 문자열로 형변환
Boolean() -> 논리형으로 형변환

암묵적 형변환
'5' + 2 -> '52' 문자열 결합
'5' - 3 -> 2 숫자로 바뀌어서 연산

===============================
0701

조건문
if (조건식) {실행할 코드}

swicth(조건값){
	case 비교값: 실행할 코드;
			break;
}

조건쪽에 falsy값이나 truthy값들이 들어갔을 때 처리

falsy값으로 되는것 : 0, '', undefined, null, NaN

반복문
for(초기식;조건식;증감식){반복실행할 코드}

화면이랑 연계
. 이벤트 처리
- 이벤트 : 클릭, 입력, 드래그, 마우스 올리기, 스크롤
- 이벤트 처리 : 이벤트가 발생했을 때 어떤 행동을 할 것인지

속성을 통한 이벤트 처리
onclick = "함수 실행"

1. JS로 태그 선택하기
- id를 이용한 선택
- css 선택자를 이용한 선택

2. 변수에 담는다
태그가 js객체로 변환되어 저장이 된다.
document.getElementById("아이디값");



